<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your TorPay Credentials</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="{{ url_for('static', filename='css/torpay-merchant.css') }}">
</head>
<body class="torpay-merchant">
  <div class="torpay-auth-container">
    <div class="torpay-auth-card torpay-animate">
      <div class="torpay-auth-logo">
        <div class="torpay-auth-logo-icon"></div>
        <h1 class="torpay-auth-title">Save your credentials</h1>
        <p class="torpay-auth-subtitle">
          These are shown once. Store them offline before continuing.
        </p>
      </div>

      {% if credentials.message %}
      <div class="torpay-alert torpay-alert-info" style="margin-bottom: 1rem;">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="16" x2="12" y2="12"/>
          <line x1="12" y1="8" x2="12.01" y2="8"/>
        </svg>
        <span>{{ credentials.message }}</span>
      </div>
      {% endif %}

      <div class="torpay-form-group">
        <label class="torpay-form-label">Login ID</label>
        <div class="torpay-code" style="font-size: 1rem;">{{ credentials.login_id }}</div>
      </div>
      <div class="torpay-form-group">
        <label class="torpay-form-label">Login Secret</label>
        <div class="torpay-code" style="font-size: 1rem;">{{ credentials.login_secret }}</div>
      </div>
      <div class="torpay-form-group">
        <label class="torpay-form-label">API Key</label>
        <div class="torpay-code" style="font-size: 1rem;">{{ credentials.api_key }}</div>
      </div>
      <div class="torpay-form-group">
        <label class="torpay-form-label">Webhook Secret</label>
        <div class="torpay-code" style="font-size: 1rem;">{{ credentials.webhook_secret }}</div>
      </div>

      <div class="torpay-alert torpay-alert-warning" style="margin: 1.25rem 0;">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 9v4"/>
          <path d="M12 17h.01"/>
          <path d="M3 21h18L12 2z"/>
        </svg>
        <span>
          Keep your security phrase safe. It's required to rotate your login secret and request payouts, and it is never displayed here.
        </span>
      </div>

      <a class="torpay-btn torpay-btn-primary torpay-btn-lg" style="width: 100%;" href="{{ url_for('merchant_auth.dashboard') }}">
        Go to dashboard
      </a>
    </div>
  </div>
</body>
</html>
